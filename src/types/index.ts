export interface IProject {
  id: string;
  project_title: string;
  image: string;
  imageScroll: string;
  technologies: string[];
  liveURL: string;
  github: string;
  features: string[];
  heading: string;
}
